# SuaveForge v7.0 화면·가시성 점검

## 정보 구조

- 첫 화면은 `업무 자동화`와 `소프트웨어 재구축`을 서로 독립된 두 블록으로 나눴습니다.
- 서로 다른 두 역량을 한 문장 안에서 해석하게 만들지 않고, 한 블록에서 한 가지 의미만 읽히도록 구성했습니다.
- 첫 화면의 중복 설명을 제거하고 핵심 문장, 실제 작업 화면, 행동 버튼만 남겼습니다.
- 프로젝트 개수처럼 추가 작업에 따라 달라질 수 있는 확정 숫자는 공개 문구에서 사용하지 않습니다.
- 전체 작업은 자동 이동과 정지 행을 섞지 않고 하나의 정적인 카드 그리드로 통일했습니다.

## 이미지 사용

- 대표 사례와 전체 작업의 프로젝트 이미지는 모두 실제 프로그램 화면을 사용합니다.
- 프로젝트 카드와 상세 갤러리에서 사용하는 로컬 이미지 경로를 전수 확인했으며 누락된 파일이 없습니다.
- 프로젝트 이미지에 SVG로 만든 대체 목업이나 임시 프로토타입 이미지를 사용하지 않습니다.
- 화면 비율이 서로 다른 이미지는 잘라 맞추지 않고 `object-fit: contain`으로 보존합니다.
- 밝거나 어두운 화면 모두 주변 배경과 분리되도록 중립색 프레임, 얇은 테두리와 제한적인 그림자를 사용합니다.
- 이미지 위에 긴 설명을 겹치지 않고 제목과 캡션을 이미지 바깥에 배치했습니다.

## 글자와 레이아웃

- 계산된 스타일 기준으로 데스크톱 화면의 사용자 노출 텍스트는 13px 미만이 없습니다.
- 390px 모바일 화면의 사용자 노출 텍스트는 14px 미만이 없습니다.
- 입력창은 모바일에서 16px 이상을 유지합니다.
- 1440px 데스크톱과 390px 모바일 렌더링에서 수평 오버플로는 0px입니다.
- 대표 사례, 전체 작업, 프로젝트 상세창과 작업 더 보기 상태를 실제 렌더링으로 점검했습니다.

## 동작과 접근성

- 전체 작업의 추가 버튼은 처음에는 일부 카드만 보여주고, 누르면 나머지 작업을 같은 그리드 안에서 펼칩니다.
- 대표 작업 확장 버튼과 프로젝트 상세 대화상자의 열기·닫기를 확인했습니다.
- 로고 SVG는 화면마다 내부 ID를 고유하게 바꿔 중복 ID가 발생하지 않습니다.
- 키보드 사용자를 위한 본문 바로가기와 포커스 표시를 유지합니다.
- `prefers-reduced-motion` 환경에서는 장식 애니메이션을 줄입니다.

## 품질보증 문구

논리 순서를 다음과 같이 유지합니다.

1. 자체 자동화 테스트로 주요 기능과 예외 조건을 반복 검증합니다.
2. 검증을 통과한 결과물만 납품합니다.
3. 만약 납품 범위에서 오류나 업데이트 문제가 발견되면 3개월간 추가 비용 없이 바로잡습니다.

오류 대응을 판촉 혜택으로 앞세우지 않고, 사전 검증에 대한 근거와 만약의 경우에 제공되는 안전장치로 표현합니다.
